---
vpcs:
  sec-nx-prd:
    routerDef:
      sec-nx-prd-c1-gcp-rtr01:
        region: us-central1
        description:
        routerASN:  # Custom ASN, if blank will pull from GLOBAL-ASN.YML
        advertiseMode: DEFAULT
        custom_advertised_ip_ranges:
          includeSubnetDef: true
          customIPList:

        ha_vpn_gateways_to_transit:
          sec-nx-prd-c1-totransit:
            description:
            if0_peer_cidr:
              cidr: 169.254.0.112/30
              bluecat-id: 1555693
              status: PROVISIONED
            if1_peer_cidr:
              cidr: 169.254.0.116/30
              bluecat-id: 1555694
              status: PROVISIONED

        outbound_nat_gateway:
          name: sec-nx-prd-c1-nat-gateway
          icmp_timeout:
          tcp_est_timeout:
          tcp_trans_timeout:
          udp_timeout:
          min_ports_per_vm:
          logConfig:
            enable: false
            filter:
          manualAddresses: 2

      sec-nx-prd-e1-gcp-rtr01:
        region: us-east1
        description:
        routerASN:  # Custom ASN, if blank will pull from GLOBAL-ASN.YML
        advertiseMode: DEFAULT
        custom_advertised_ip_ranges:
          includeSubnetDef: true
          customIPList:

        ha_vpn_gateways_to_transit:
          sec-nx-prd-e1-totransit:
            description:
            if0_peer_cidr:
              cidr: 169.254.0.120/30
              bluecat-id: 1555695
              status: PROVISIONED
            if1_peer_cidr:
              cidr: 169.254.0.124/30
              bluecat-id: 1555696
              status: PROVISIONED

        outbound_nat_gateway:
          name: sec-nx-prd-e1-nat-gateway
          icmp_timeout:
          tcp_est_timeout:
          tcp_trans_timeout:
          udp_timeout:
          min_ports_per_vm:
          logConfig:
            enable: false
            filter:
          manualAddresses: 2
