---
storageBuckets:
  gitlab-ocp-cache:
    permissions:
      serviceAccountsByEmail:
        gitlab-ocp-cache@kohls-devexp-tools-prd.iam.gserviceaccount.com:
          deletionProtect: false
          roles:
          - storage.objectAdmin
