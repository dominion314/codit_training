---
##THIS CONFIG IS TYPICALLY GENERATED BY CLOUD NETWORK TEAM
vpcs:
  cpesample-nx-lle: # vpc name
    routerDef: # vpn and cloud NAT routers belonging to this VPC
      # beginning of router object; name= vpc + region + "gcp" + router#
      cpesample-nx-lle-c1-gcp-rtr01:
        region: us-central1 # GCP region resource is deployed in
        description:
        routerASN: # optional: hard code ASN from router; ALWAYS DEFINED IN GLOBAL VARS
        advertiseMode: DEFAULT # advertises all subnets within VPC
        custom_advertised_ip_ranges:
          includeSubnetDef: true
          customIPList:
        # name of VPN's on this router to the transit project within GCP for
        # inter-project communication
        ha_vpn_gateways_to_transit:
          cpesample-nx-lle-c1-totransit: # vpn router name
            description:
            # list of vpn router interfaces
            if0_peer_cidr: # interface address
              cidr: # auto generated
              bluecat-id: # auto generated
              status: # auto generated
            if1_peer_cidr: # interface address
              cidr: # auto generated
              bluecat-id: # auto generated
              status: # auto generated
      cpesample-nx-lle-e1-gcp-rtr01:
        region: us-east1
        description:
        routerASN:
        advertiseMode: DEFAULT
        custom_advertised_ip_ranges:
          includeSubnetDef: true
          customIPList:
        ha_vpn_gateways_to_transit:
          cpesample-nx-lle-e1-totransit:
            description:
            if0_peer_cidr:
              cidr: # auto generated
              bluecat-id: # auto generated
              status: # auto generated
            if1_peer_cidr:
              cidr: # auto generated
              bluecat-id: # auto generated
              status: # auto generated
