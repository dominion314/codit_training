---
iamServiceAccounts:
  image-build:
    displayName: image-build
    description: Used for building images
  jenkins:
    displayName: jenkins
    description: Used for running the jenkins instance
  terraform-devops:
    displayName: terraform-devops
    description: Used for deploying and managing compute engine instances
  yum-repo:
    displayName: yum-repo
    description: Used for running the yum repo instances

