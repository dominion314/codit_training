---
storageBuckets:
  gitlab-ocp-cache:
    permissions:
      serviceAccountsByEmail:
        gitlab-ocp-cache@doms-devexp-tools-prd.iam.gserviceaccount.com:
          deletionProtect: false
          roles:
          - storage.objectAdmin
