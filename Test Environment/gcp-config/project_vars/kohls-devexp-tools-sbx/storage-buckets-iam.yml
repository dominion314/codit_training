---
storageBuckets:
  gitlab-ocp-cache-sbx:
    permissions:
      serviceAccountsByEmail:
        gitlab-ocp-cache@doms-devexp-tools-sbx.iam.gserviceaccount.com:
          deletionProtect: false
          roles:
          - storage.objectAdmin
